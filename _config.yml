title: MeetMi
description: Privacy Policy and Support for MeetMi Managed AI on the Mac App Store.
# `layout: default` has to come from somewhere; this is one of the themes
# GitHub Pages builds without a Gemfile. Primer over minimal: minimal's sidebar
# puts "View the Project on GitHub / maintained by kinlu" beside a privacy
# policy, which reads like a code project rather than a product's legal page.
theme: jekyll-theme-primer
show_downloads: false
exclude:
  - README.md
